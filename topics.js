function toggleDropdown(topicId){const dropdown=document.getElementById(`${topicId}-dropdown`);const allDropdowns=document.querySelectorAll('.dropdown');allDropdowns.forEach(d=>d.classList.remove('active'));if(dropdown&&!dropdown.classList.contains('active')){dropdown.classList.add('active')}}
document.addEventListener('DOMContentLoaded',function(){const topics=document.querySelectorAll('.topic');topics.forEach(topic=>{topic.addEventListener('click',function(event){const topicId=this.getAttribute('onclick').match(/toggleDropdown\('([^']+)'\)/)[1];toggleDropdown(topicId);event.stopPropagation()})});document.addEventListener('click',function(event){const isTopic=event.target.classList.contains('topic');if(!isTopic){const allDropdowns=document.querySelectorAll('.dropdown');allDropdowns.forEach(dropdown=>dropdown.classList.remove('active'))}})})